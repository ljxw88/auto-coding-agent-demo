{
  "project_name": "CausalIQ GatedDeltaNet",
  "research_goal": "Improve classification accuracy on IQ signal device fingerprinting (LocD dataset) beyond the current SOTA of 82-84% using the GatedDeltaNet architecture. Target 86%+. Focus on patch embedding improvements, preprocessing strategies, and architecture tweaks within gateddeltanet. NOTE: dropout=0 and weight_decay=0 are known to be optimal — do NOT increase them.",
  "research_domain": "RF signal processing IQ classification state space models sequence classification",
  "arxiv_search_queries": [
    "state space model sequence classification time series",
    "patch embedding signal processing deep learning fingerprinting"
  ],

  "primary_metric": "accuracy",
  "metric_direction": "higher_is_better",

  "smoke_test_command_template": "cd experiment_project && python train_causal_iq_classifier.py --manifest ./shards/manifest.json --arch gateddeltanet --batch_size 128 --epochs {epochs} --lr 3e-4 --dim 512 --depth 4 --heads 8 --pooling last --patch_size 64 --weight_decay 0.0 --save_path ./saved_models/smoke_test.pth --patch_embed_type causal_conv --augment --compile",
  "full_train_command_template": "cd experiment_project && python train_causal_iq_classifier.py --manifest ./shards/manifest.json --arch gateddeltanet --batch_size 128 --epochs {epochs} --lr 3e-4 --dim 512 --depth 4 --heads 8 --pooling last --patch_size 64 --weight_decay 0.0 --save_path ./saved_models/current_experiment.pth --patch_embed_type causal_conv --augment --compile --wandb --wandb_project causal_iq_cls_patchemb --wandb_name {wandb_name}",
  "eval_command": "cd experiment_project && python test_causal_iq_classifier.py --tx_start 35 --tx_end 45 --pkt_start 0 --pkt_end 100 --device auto --normalize --data_path /LOCAL/data/n210_2_leg_LocD.h5 --model_path ./saved_models/current_experiment.pth --output ../tmp_metrics.json",
  "metrics_output_file": "tmp_metrics.json",

  "smoke_test_epochs": 10,
  "full_train_epochs": 600,
  "max_seconds_per_epoch": 10,

  "max_experiments": 50,
  "max_consecutive_failures": 5,
  "target_metric_value": null,

  "idea_generation": {
    "use_arxiv": true,
    "ideas_per_batch": 5,
    "llm_model": "claude-opus-4-5"
  }
}
