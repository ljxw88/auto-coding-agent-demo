{
  "project_name": "CIFAR-10 SmallCNN",
  "research_goal": "Improve test accuracy of the SmallCNN baseline on CIFAR-10 (10K training subset)",
  "research_domain": "image classification deep learning",
  "arxiv_search_queries": [
    "CNN training tricks CIFAR-10 accuracy improvement",
    "batch normalization regularization small dataset"
  ],

  "primary_metric": "accuracy",
  "metric_direction": "higher_is_better",

  "train_command_template": "cd example-research && conda run -n llm python train.py --epochs {epochs}",
  "eval_command": "cd example-research && conda run -n llm python eval.py --output results.json",
  "metrics_output_file": "example-research/results.json",

  "smoke_test_epochs": 10,
  "full_train_epochs": 500,
  "max_seconds_per_epoch": 10,

  "max_experiments": 50,
  "max_consecutive_failures": 5,
  "target_metric_value": null,

  "idea_generation": {
    "use_arxiv": true,
    "ideas_per_batch": 5,
    "llm_model": "claude-opus-4-5"
  }
}
